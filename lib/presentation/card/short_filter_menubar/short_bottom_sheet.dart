import 'package:flutter/material.dart';

class ShortBottomSheet extends StatelessWidget {
  const ShortBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Short');
  }
}
